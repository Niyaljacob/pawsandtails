import 'package:flutter/material.dart';

class ProductsAcceptesUser extends StatelessWidget {
  const ProductsAcceptesUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('ProductsAcceptesUser'),
    );
  }
}