import 'package:flutter/material.dart';

class PaymentCashProduct extends StatelessWidget {
  const PaymentCashProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cash on Delevery'),
      ),
    );
  }
}