import 'package:flutter/material.dart';

class ProductsDeclineUser extends StatelessWidget {
  const ProductsDeclineUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('ProductsDeclineUser'),
    );
  }
}